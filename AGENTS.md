# AGENTS.md

## Project Basics

- Python project managed with `uv`.
- Run tests with `uv run pytest`.

## Common Commands

- Install deps: `uv sync`
- Run tests: `uv run pytest`

## Notes

- Use `uv` for running scripts and tests to ensure the correct environment.
